# Server Configuration
PORT=3000                    # Default port for the server
HOST=0.0.0.0                # Bind to all network interfaces (use 127.0.0.1 for localhost only)
SERVER_NAME=Simple-API      # Name displayed in logs and responses

# Performance Settings
BODY_LIMIT=1024             # Maximum request body size in bytes (keeps memory usage low)
REQUEST_TIMEOUT=30000       # Kill requests that take longer than 30 seconds

# Security Settings
TRUST_PROXY=false           # Don't trust proxy headers (set true if behind load balancer)
ENABLE_LOGGING=false        # Disable request logging for better performance (set true for debugging)

# File System
PUBLIC_DIR=public           # Directory containing HTML files to serve

# API Security
HEALTH_CHECK_API_KEY=secure-health-api-key-2024  # API key for /health endpoint

# Performance Settings (57,893 req/sec tested on Performance Core 0)
SINGLE_CORE_MODE=true           # Force single CPU core usage (Windows tested, Linux untested)
CPU_CORE_NUMBER=0               # Which CPU core to use (0-23, only if SINGLE_CORE_MODE=true)
THREAD_POOL_SIZE=1              # Node.js thread pool size (1 = single thread)